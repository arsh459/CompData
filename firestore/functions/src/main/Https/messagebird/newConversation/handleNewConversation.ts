import { ConversationMBInterface, ContactMBInterface } from "../interface";
// import { parseNewQuery } from "../newMessage/parsing/newQuery";

export const handleNewConversation = (
  conversation: ConversationMBInterface,
  contact: ContactMBInterface,
  query: string,
) => {
  //   const parsedQuery = parseNewQuery(query);
  //   console.log("parsed", parsedQuery);
  // parse initial Query
  // send new conversation bot-reply
  // create new conversation obj
  // create new contact obj
};
