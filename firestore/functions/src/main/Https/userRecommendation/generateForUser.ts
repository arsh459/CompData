import { Task } from "../../../models/Task/Task";
// import { getUserById } from "../../../models/User/Methods";

export const generateRecsForUser = async (
  uid: string,
  tasksForGame: { [gameId: string]: Task[] },
) => {
  // const user = await getUserById(uid);
  // get games
  // get tasks for game
  //
};
