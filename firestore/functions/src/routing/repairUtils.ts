// import {TripElementInterface, TripState, TripObjInterface} from '../models/TripObj/Trip';
// import {ListingInterface} from '../models/ListingObj/Listing';



export const repaitTour = () => {
    // get task before deleted


}


/*
const getPreviousTask = (
    trip: TripObjInterface, 
    allTasks: {[name: string]: ListingInterface},
    index: number,
    tripState: TripState,
    windowId: string) => {

    // initial task
    if (index === 0){
        return {
            lat: trip.tripState.starts[windowId].lat,
            lng: trip.tripState.starts[windowId].lng,
            timeStart: trip.operatingWindows[windowId]
        }
        

    }

    return {
        lat: allTasks[trip.trip[windowId][index-1].listingId].lat,
        lng: allTasks[trip.trip[windowId][index-1].listingId].lng,
        timeStart: trip.trip[windowId][index-1].timeStart        
    }
}

*/