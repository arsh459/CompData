import { View } from "react-native";
import React from "react";
import UnlockedLeagueMain from "@modules/UnlockedLeague";

const UnlockedLeagueScreen = () => {
  return (
    <View className="flex-1 bg-[#232136]">
      <UnlockedLeagueMain />
    </View>
  );
};

export default UnlockedLeagueScreen;
