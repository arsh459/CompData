export const tmp = () => console.log("rn firebase now");

// import { initializeApp, getApps, getApp } from "firebase/app";
// import { NEXT_PUBLIC_FIREBASE_CONFIG } from "react-native-dotenv";
// console.log(NEXT_PUBLIC_FIREBASE_CONFIG, "NEXT_PUBLIC_FIREBASE_CONFIG");

// const firebaseConfig = JSON.parse(
//   NEXT_PUBLIC_FIREBASE_CONFIG ? NEXT_PUBLIC_FIREBASE_CONFIG : "{}"
// );

// const firebase =
//   getApps().length === 0 ? initializeApp(firebaseConfig) : getApp();

// export default firebase;

// const firebaseConfig = JSON.parse(
//   NEXT_PUBLIC_FIREBASE_CONFIG ? NEXT_PUBLIC_FIREBASE_CONFIG : "{}"
// );

// const firebase =
//   getApps().length === 0 ? initializeApp(firebaseConfig) : getApp();

// export default firebase;

// import { initializeFirestore } from "firebase/firestore";
// export const db = initializeFirestore(firebase, {
//   experimentalForceLongPolling: true,
// });

// import { getAuth } from "firebase/auth";
// export const auth = getAuth(firebase);
