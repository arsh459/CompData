import { SessionV2 } from "@models/Event/Event";

interface Props {
  session: SessionV2;
}

const ProgramElement: React.FC<Props> = ({}) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default ProgramElement;
