import { Post } from "@models/Posts/Post";

interface Props {
  post: Post;
  unlocked?: boolean;
}

const WorkoutCard: React.FC<Props> = () => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default WorkoutCard;
