import { syncSakhi } from "@constants/icons/iconURLs";

const SyncSakhiImage = () => {
  return (
    <div className="absolute inset-0 z-10">
      <img src={syncSakhi} className="object-contain" alt="SyncSakhiImage" />
    </div>
  );
};

export default SyncSakhiImage;
